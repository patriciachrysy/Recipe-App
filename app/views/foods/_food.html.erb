<tr>
    <td><%= food.name %></td>
    <td><%= food.measurement_unit %></td>
    <td>$<%= food.price %></td>
    <td>
        <%= link_to "Modify", edit_food_path(food) if can? :update, food %>
        <%= link_to "Delete", food_path(food), data: {
                turbo_method: :delete,
                turbo_confirm: 'Are you sure you want to delete this food?'
            } if can? :destroy, food %>
    </td>
</tr>